server:
  port: 8441

spring:
  application:
    name: gr-https-ssl-ms1
  # Connect to the config server to get the properties
  cloud:
    config:
      uri: ${CONFIG_SERVER_ADDR:http://localhost:8762}
      #uri: ${CONFIG_SERVER_ADDR:http://localhost:8443}